import Foundation
import SwiftData

class DataSeeder {
    // DataSeeder is no longer needed - using SampleDataService instead
    static func seedDataIfNeeded(modelContext: ModelContext) {
        // Empty - seeding is done via SampleDataService.seedDataIfNeeded
    }
}
